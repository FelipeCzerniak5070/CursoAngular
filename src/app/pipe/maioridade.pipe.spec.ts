import { MaioridadePipe } from './maioridade.pipe';

describe('MaioridadePipe', () => {
  it('create an instance', () => {
    const pipe = new MaioridadePipe();
    expect(pipe).toBeTruthy();
  });
});
